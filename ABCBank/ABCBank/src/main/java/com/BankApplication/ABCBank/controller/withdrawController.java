package com.BankApplication.ABCBank.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class withdrawController {
    //withdraw money (Assume user already log in (Key in phone number)
}
